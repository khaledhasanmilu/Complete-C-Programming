//Question: Program that will print “Hello World”.

#include <stdio.h>               // This line includes the standard input-output library, which is necessary for using printf function.
 
int main() {                   // This is the main function where the execution of the program begins.
    printf("Hello World!");   // This line prints the string "Hello World!" to the console.
    return 0;                 // This line indicates that the program ended successfully. The return value of 0 is a common convention to indicate successful completion.
}
