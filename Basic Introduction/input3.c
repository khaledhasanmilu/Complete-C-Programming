#include <stdio.h>
int main()
{
  int first;
  int mid;
  int last;
  printf("Enter three integer value:");
  scanf("%d", &first);
  scanf("%d", &mid);
  scanf("%d", &last);
  printf("First value: %d\n", first);
  printf("Last value: %d\n", last);

  return 0;
}